import 'package:flutter/material.dart';

class AppColors {
  // Tropical Vibes
  static const Color primary = Color(0xFF008080);
  static const Color secondary = Color(0xFFFFD700);
  static const Color accent = Color(0xFFFC74FD);
  static const Color background = Color(0xFFFFF5EE);
}
